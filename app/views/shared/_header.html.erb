<nav class="navbar fixed-top navbar-expand-md navbar-dark bg-chacon no-print">
  <a class="navbar-brand" href="/">RevvedHR</a>
  <div class="header-items">
    <div>
      <% if user_signed_in? %>
        <ul class="navbar-nav">
          <% if current_user.has_role? :Admin or current_user.has_role? :Manager %>
            <li class="nav-item">
              <%= link_to 'Employees', employees_path, class: 'nav-link' %>
            </li>
          <% end %>
          <% if current_user.has_role? :Admin %>
            <li class="nav-item">
              <%= link_to 'Transactions', transactions_path, class: 'nav-link' %>
            </li>
          <% end %>
          <% if current_user.has_role? :Super %>
            <li class="nav-item">
              <%= link_to 'Super', admin_root_path, class: 'nav-link' %>
            </li>
          <% end %>
        </ul>
      <% end %>
    </div>
    <div>
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <% if current_user.employee %>
            <li class="nav-item pr-2 text-light">
              <%= link_to current_user.employee.first_name, users_path, method: :get, class: 'nav-link' %>
            </li>
            <li class="nav-item pr-2 pt-2 text-light">
              <%= current_user.organization %>
            </li>
          <% end %>
          <% if current_user != true_user %>
            <li class="nav-item pr-2 text-light">
              <%= button_to "Back to admin", stop_impersonating_users_path, method: :post, class: 'nav-link btn btn-link' %>
            </li>
          <% end %>
          <li class="nav-item">
            <%= link_to 'Log out', destroy_user_session_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
<div id="cover" class="no-print"></div>